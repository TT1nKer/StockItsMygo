# Observation Mode Guide
# 观察模式使用指南

## 概述 Overview

这是为**交易前练习**设计的每日工作流程系统。在你开始真实交易之前，这个系统帮你：

- 每天自动发现值得关注的股票
- 下载和分析详细数据
- 使用多种策略进行对比
- 生成结构化的每日报告
- 积累经验和信心

**重点**: 这是观察模式，**不会进行任何实际交易**。

---

## 快速开始 Quick Start

### 每天只需一个命令

```bash
python daily_observation.py
```

这个命令会：
1. 扫描市场寻找高动量股票（分数 >= 70）
2. 自动将高分股票（>= 80）添加到观察列表
3. 下载观察列表股票的5分钟数据
4. 运行高级技术分析（20+指标）
5. 对比3种策略（动量、均值回归、突破）
6. 生成详细的每日报告

**执行时间**: 约5-10分钟（取决于观察列表大小）

---

## 报告结构 Report Structure

报告按照 **年/月/周** 组织，方便回顾：

```
reports/
  2025/
    01-January/
      Week_01/
        2025-01-01_Monday.md
        2025-01-02_Tuesday.md
        2025-01-03_Wednesday.md
      Week_02/
        2025-01-06_Monday.md
        ...
    02-February/
      Week_05/
        ...
```

### 为什么按周组织？

- 股市是按周运作的（周一到周五）
- 容易对比一周内的表现
- 方便回顾周末总结

---

## 每日报告内容 Report Content

每份报告包含以下章节：

### 1. Executive Summary（执行摘要）
- 发现了多少机会
- 观察列表有多少股票
- 今天新增了多少
- 用了哪些策略

### 2. Top Momentum Opportunities（顶级动量机会）
- 前10名高动量股票
- 详细的交易计划（进场价、止损、止盈）
- 选择原因分析

**示例**:
```markdown
| Rank | Symbol | Score | Signal | Momentum | Entry | Stop Loss | Take Profit |
|------|--------|-------|--------|----------|-------|-----------|-------------|
| 1    | NVDA   | 95    | BUY    | +12.5%   | $520  | $494      | $582        |
```

### 3. Watchlist - Deep Analysis（观察列表深度分析）
为每只观察列表中的股票提供：

- **技术指标**:
  - ADX (趋势强度)
  - RSI (超买超卖)
  - Stochastic (动量震荡)
  - ATR (波动性)
  - OBV (成交量趋势)
  - VWAP (成交量加权均价)

- **K线形态**: 锤子线、射击之星、十字星、吞没形态等

- **支撑阻力位**: Pivot Points (R1/R2/R3, S1/S2/S3)

- **综合评分**: 0-100分

### 4. Multi-Strategy Comparison（多策略对比）
对比3种策略在同一股票上的表现：

- **Momentum Strategy** (动量策略): 适合上升趋势
- **Mean Reversion Strategy** (均值回归): 适合震荡行情
- **Breakout Strategy** (突破策略): 适合盘整后突破

**示例**:
```markdown
### AAPL
| Strategy        | Signal      | Score | Confidence |
|-----------------|-------------|-------|------------|
| momentum        | STRONG_SELL | 0     | 0.0%       |
| mean_reversion  | STRONG_BUY  | 80    | 85.0%      |
| breakout        | HOLD        | 50    | 60.0%      |
```

### 5. Daily Observations（每日观察）
**手动填写区域** - 这是给你记录观察的：

- 今天的市场状况（牛市/熊市/震荡/高波动）
- 你的个人笔记
- 你注意到的模式
- 如果你在交易，你会怎么做

### 6. Learning Points（学习要点）
**跟踪学习进度**:
- 策略表现观察
- 形态识别练习
- 风险管理回顾
- 市场情绪分析

---

## 工作流程详解 Detailed Workflow

### Step 1: 更新日线数据
```
[INFO] Skipping full update - using existing data
       To update all stocks, run: python script/update_data.py
```

注意: 首次运行时，你可能需要先更新所有股票数据。

### Step 2: 动量扫描
```python
scanner = MomentumScanner()
signals = scanner.scan(
    min_score=70,        # 最低分数 70
    min_volume=500000,   # 最低成交量 50万
    sort_by='momentum_score',
    limit=20             # 返回前20名
)
```

**评分标准** (0-100):
- 动量 (40分): 20天价格涨幅
- 成交量 (25分): 成交量比率
- 突破 (20分): 是否突破20日高点
- 近期强势 (15分): 最近5天表现

### Step 3: 更新观察列表
自动将高分股票添加到观察列表：

- 分数 >= 90: 高优先级 (Priority 1)
- 分数 80-89: 中优先级 (Priority 2)
- 已存在的股票会跳过（不重复添加）

你也可以手动管理观察列表：
```python
from script.watchlist import WatchlistManager
mgr = WatchlistManager()

# 添加股票
mgr.add('MSFT', priority=1, notes='Tech leader')

# 移除股票
mgr.remove('AAPL')

# 查看列表
mgr.print_summary()
```

### Step 4: 下载5分钟数据
为观察列表中的所有股票下载最近7天的5分钟数据：

```python
symbols = ['AAPL', 'MSFT', 'NVDA']
db.batch_download_intraday(
    symbols=symbols,
    interval='5m',    # 5分钟间隔
    period='7d',      # 最近7天
    workers=3         # 3个并发线程
)
```

### Step 5: 高级分析
对每只股票运行20+技术指标分析：

- 趋势指标: ADX, Moving Averages, MACD
- 动量指标: RSI, Stochastic, Momentum
- 波动指标: ATR, Bollinger Bands
- 成交量指标: OBV, Volume Ratio, VWAP
- 形态识别: 4种K线形态
- 支撑阻力: Pivot Points系统

### Step 6: 策略对比
使用3种策略分析同一股票，看看哪个策略给出更好的信号。

### Step 7: 生成报告
将所有分析结果整理成结构化的Markdown报告。

---

## 如何使用报告学习 How to Learn from Reports

### 每天的工作流程

1. **早上运行脚本** (开盘前):
   ```bash
   python daily_observation.py
   ```

2. **阅读报告** (10-15分钟):
   - 看看有哪些高分机会
   - 阅读分析原因
   - 注意技术指标的数值

3. **添加你的观察**:
   - 市场今天是什么状态？
   - 你认为哪些股票看起来最好？
   - 如果你在交易，你会买哪只？为什么？
   - 你的止损和止盈会设在哪里？

4. **第二天早上**:
   - 打开昨天的报告
   - 看看你关注的股票表现如何
   - 哪个策略更准确？
   - 你的判断对了吗？

### 每周回顾

周末时，回顾本周的所有报告：

```bash
cd reports/2025/01-January/Week_01/
```

对比：
- 哪些股票整周表现最好？
- 哪个策略本周最准确？
- 你的观察和实际结果的差距在哪？
- 你学到了什么新模式？

### 每月总结

每月底，创建一个总结：
- 本月发现了多少机会？
- 哪些类型的股票表现最好？
- 你的预测准确率如何？
- 你最大的学习点是什么？

---

## 观察列表管理 Watchlist Management

### 手动管理

```python
from script.watchlist import WatchlistManager
mgr = WatchlistManager()

# 查看当前列表
mgr.print_summary()

# 添加股票
mgr.add('TSLA', priority=1, notes='EV leader, high volatility')
mgr.add('AMD', priority=2, notes='Chip sector')

# 更新价格目标
mgr.update_prices('TSLA', target_price=250.0, stop_loss=180.0)

# 移除股票
mgr.remove('AAPL')

# 导出/导入
mgr.export_to_csv('my_watchlist.csv')
mgr.import_from_csv('my_watchlist.csv')
```

### 自动管理

系统会自动添加高分股票，但你可以自定义：

```python
# 更严格的标准（只要90+分）
mgr.auto_add_from_momentum(min_score=90, max_additions=5)

# 更宽松的标准（70+分）
mgr.auto_add_from_momentum(min_score=70, max_additions=20)
```

### 优先级系统

- **Priority 1 (High)**: 最关注，每天必看
- **Priority 2 (Medium)**: 中等关注
- **Priority 3 (Low)**: 低关注，偶尔看看

---

## 策略系统 Strategy System

### 三种内置策略

#### 1. Momentum Strategy (动量策略)
**适用**: 牛市、上升趋势

**逻辑**:
- 买入20天强势上涨的股票
- 成交量放大
- 突破20日高点
- 价格在均线上方

**参数**:
- 回看天数: 20天
- 最小动量: 10%
- 成交量倍数: 1.5x
- 止损: -5%
- 止盈: +12%

#### 2. Mean Reversion Strategy (均值回归策略)
**适用**: 震荡市、箱体整理

**逻辑**:
- 买入超卖的股票（RSI < 30）
- 价格接近布林带下轨
- 偏离MA20较远
- 期待反弹回中位

**参数**:
- RSI阈值: 30 (超卖)
- 布林带周期: 20
- 止损: -7%
- 止盈: 回到中轨

#### 3. Breakout Strategy (突破策略)
**适用**: 盘整后、关键阻力位

**逻辑**:
- 检测20天盘整（低波动）
- 突破确认（价格 > 最高点 2%）
- 成交量放大确认
- 使用ATR设置动态止损

**参数**:
- 盘整天数: 20
- 突破确认: 2%
- 成交量倍数: 1.5x
- ATR倍数: 2x

### 如何选择策略

系统会自动对比所有策略，但你可以根据市场状况手动选择：

```python
from script.strategy_manager import StrategyManager
mgr = StrategyManager()

# 获取推荐
rec = mgr.recommend_strategy('trending')  # 趋势市
rec = mgr.recommend_strategy('ranging')   # 震荡市
rec = mgr.recommend_strategy('volatile')  # 波动市

# 使用特定策略
strategy = mgr.get_strategy('momentum')
signal = strategy.analyze('AAPL')

# 对比所有策略
comparison = mgr.compare_strategies('AAPL')
```

### 自定义策略

你可以创建自己的策略！查看模板：
```python
# 复制模板
cp script/strategies/custom_template.py script/strategies/my_strategy.py

# 编辑你的策略
# 实现 analyze() 方法
# 返回标准信号格式
```

详见: [docs/策略系统使用指南.md](策略系统使用指南.md)

---

## 常见问题 FAQ

### Q1: 报告太长了，能简化吗？

可以！编辑 `tools/daily_workflow.py`，调整：
- 减少分析的股票数量（`watchlist[:10]` 改为 `watchlist[:5]`）
- 只显示前5名机会（`momentum_signals[:10]` 改为 `[:5]`）
- 去掉某些章节

### Q2: 能每天自动运行吗？

可以！两种方法：

**方法1: Windows任务计划程序**
```batch
# 创建批处理文件: run_daily.bat
@echo off
cd /d d:\strategy=Z
python daily_observation.py >> logs\daily_%date%.log 2>&1
```

然后设置任务计划在每天早上8点运行。

**方法2: Linux/Mac crontab**
```bash
# 编辑 crontab
crontab -e

# 添加这行（每天早上8点）
0 8 * * 1-5 cd /path/to/strategy=Z && python daily_observation.py
```

### Q3: 数据更新很慢怎么办？

第一次会慢，因为要下载所有历史数据。之后每天只更新最新的，会快很多。

优化方法：
- 减少观察列表大小
- 增加并发线程数（`workers=5`）
- 只更新必要的股票

### Q4: 什么时候可以开始真实交易？

建议：
- **至少观察2-4周**
- 你能预测60%+的股票走势
- 你理解每种策略的优缺点
- 你知道如何设置止损止盈
- 你有风险管理计划

当你准备好时，告诉系统，它会给你实际交易建议（设置limit order等）。

### Q5: 能分析美股以外的市场吗？

目前系统使用yfinance，支持：
- 美股 (NASDAQ, NYSE)
- 加密货币 (BTC-USD, ETH-USD)
- 外汇 (EURUSD=X)
- 其他市场（需要确认yfinance支持）

修改 `db/api.py` 中的ticker symbol即可。

---

## 进阶技巧 Advanced Tips

### 1. 创建自己的筛选器

```python
# 只关注科技股
tech_stocks = ['AAPL', 'MSFT', 'GOOGL', 'NVDA', 'AMD', 'INTC']
scanner = MomentumScanner()
signals = scanner.scan_symbols(tech_stocks, min_score=70)
```

### 2. 自定义评分权重

编辑策略文件，调整权重：
```python
# script/strategies/momentum.py
score = (
    momentum_score * 0.5 +  # 改为50%权重
    volume_score * 0.3 +
    breakout_score * 0.15 +
    recent_score * 0.05
)
```

### 3. 添加更多技术指标

在 `script/advanced_analysis.py` 添加新指标：
```python
def calculate_my_indicator(self, df):
    # 你的自定义指标
    pass
```

### 4. 导出数据到Excel

```python
# 在报告生成后
import pandas as pd
df = pd.DataFrame(momentum_signals)
df.to_excel(f'reports/{today}_signals.xlsx', index=False)
```

---

## 目录结构 Directory Structure

```
d:/strategy=Z/
├── daily_observation.py      # 主入口（运行这个）
├── quick_start.py            # 原始快速开始示例
├── db/
│   ├── stock.db              # SQLite数据库（1.5GB）
│   ├── api.py                # 数据库API
│   └── init_db.py            # 数据库初始化
├── script/
│   ├── momentum_scanner.py   # 动量扫描器
│   ├── watchlist.py          # 观察列表管理
│   ├── advanced_analysis.py  # 高级技术分析
│   ├── strategy_base.py      # 策略框架
│   ├── strategy_manager.py   # 策略管理器
│   └── strategies/           # 策略实现
│       ├── momentum.py
│       ├── mean_reversion.py
│       ├── breakout.py
│       └── custom_template.py
├── tools/
│   └── daily_workflow.py     # 工作流程编排器
├── reports/                  # 每日报告（自动生成）
│   └── 2025/
│       └── 01-January/
│           └── Week_01/
│               ├── 2025-01-01_Monday.md
│               └── ...
└── docs/
    ├── OBSERVATION_MODE_GUIDE.md  # 本文档
    └── 策略系统使用指南.md
```

---

## 开始你的学习之旅！

现在运行：
```bash
python daily_observation.py
```

每天坚持，几周后你会发现自己对市场的理解大大提升！

记住：
- **耐心观察，不要急于交易**
- **记录你的观察和想法**
- **对比预测和结果**
- **从错误中学习**
- **建立你的交易系统**

祝你学习愉快！ 🚀📈